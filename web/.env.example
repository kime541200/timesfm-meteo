# API key used by the browser during local development.
# This value is visible in browser runtime; do not use it for public deployment.
VITE_TIMESFM_API_KEY=

# FastAPI server target for Vite dev proxy.
VITE_TIMESFM_API_TARGET=http://localhost:8000
